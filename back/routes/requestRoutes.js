const express = require('express');
// const { getProduitsFollowers } = require('../controllers/productController');
const router = express.Router();

// router.get('/:base/produits-followers/:userId', getProduitsFollowers);
// router.get('/:base/produits-followers/:userId/:produitId?niveau=n', getProduitsFollowers);
// router.get('/:base/produit-viral/:produitId?niveau=n', getProduitsFollowers);

module.exports = router;